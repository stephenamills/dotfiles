interface:
  display_name: "Batch Study Guide — Finance"
  short_description: "Generate and install finance guides and maps"
  default_prompt: "Use $batch-study-guide-finance to generate all missing finance guides and dependency-ordered course maps, installing every validated file immediately and reporting each canonical path."
