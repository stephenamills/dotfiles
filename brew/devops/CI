brew "act" # Run your GitHub Actions locally
brew "action-validator" # Tool to validate GitHub Action and Workflow YAML files
brew "actionlint" # Static checker for GitHub Actions workflow files
brew "actions-batch" # Time-sharing supercomputer built on GitHub Actions

# brew "dagger" # runs your pipeline in containers locally or remote, and with reusable functions and modules
# brew "woodpecker-cli"

# brew "frizbee" # creates a checksum of a GitHub Actions workflow file or Docker image
# brew "hermit" # by Cash App - installs tools in isolated sets for CI environments etc.
# brew "release-it" # automate versioning and package publishing tasks
# brew "renovate" # automated dependency updates
# brew "source-to-image" # by Red Hat - something about reproducible builds

# brew "argocd" # GitOps Continuous Delivery for Kubernetes
# brew "buildpulse-test-reporter" # Connect your CI to BuildPulse to detect, track, and rank flaky tests
# brew "circleci" # Enables you to reproduce the CircleCI environment locally
# brew "drone-cli" # Command-line client for the Drone continuous integration server
# brew "gitlab-runner" # Official GitLab CI runner
# brew "jenkins" # Extendable open source continuous integration server
# brew "osc" # Command-line interface to work with an Open Build Service
# brew "stepci" # API Testing and Monitoring made simple
# brew "tart" # macOS and Linux VMs on Apple Silicon to use in CI and other automation
# brew "woodpecker-cli" # CLI client for the Woodpecker Continuous Integration server

# brew "dagger" # Portable devkit for CI/CD pipelines
# brew "gitlab-ci-local" # Run gitlab pipelines locally as shell executor or docker executor
# brew "poutine" # Security scanner that detects vulnerabilities in build pipelines
# brew "prqlc" # Simple, powerful, pipelined SQL replacement
# brew "resty" # Command-line REST client that can be used in pipelines
# brew "syslog-ng" # Log daemon with advanced processing pipeline and a wide range of I/O methods
# brew "truffle" # Development environment, testing framework and asset pipeline for Ethereum
# brew "vip" # Program that provides for interactive editing in a pipeline